## PaperQuery

PaperQuery is a RAG (Retrieval-Augmented Generation) application for Chat with PDF, built with a Next.js client and a Node.js server for PDF ingestion and querying.

## Structure

- `client/`: Next.js frontend
- `server/`: Node.js backend, queues, and workers